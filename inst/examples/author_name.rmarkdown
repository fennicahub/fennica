---
title: "Author's info: name"
hide-description: false
description: ""
---

```{r}
#| include: FALSE
source("init.R")
source("author_name.R")
source("author_name_700.R")
```




## Author name for Language material (books) in 100,a - field. 

MARC: [100a](https://www.loc.gov/marc/bibliographic/bd100.html) and [700a](https://www.loc.gov/marc/bibliographic/bd100.html)

Author's name section's summary tables offer insights into the dataset's integrity, illustrating the accepted and discarded author names. An examination of missing values in the original dataset provides transparency regarding data completeness. The inclusion of information on name variants and pseudonyms enriches the analysis, addressing nuances in authorship representation. This comprehensive approach ensures a thorough understanding of the dataset's composition and the intricacies associated with author identification. There are `r sum(is.na(df.orig[[field]]))` records with missing author name information in the original raw data.

### Complete Dataset Overview for 100 field

-   [Unique accepted entries in original data](dataTable/data_table.html?path=https://a3s.fi/swift/v1/AUTH_3c0ccb602fa24298a6fe3ae224ca022f/fennica-container/output.tables/author_name_accepted.csv): `r length(unique(df$author_name))`
-   [Unique discarded entries in original data](dataTable/data_table.html?path=https://a3s.fi/swift/v1/AUTH_3c0ccb602fa24298a6fe3ae224ca022f/fennica-container/output.tables/author_name_discarded.csv) (excluding NA cases): `r as.vector(countLines(file_discarded) - 1)`
-   Original documents with non-NA titles `r sum(!is.na(df$author_name))` / `r nrow(df)` (`r round(100*mean(!is.na(df$author_name)), 1)`%)
-   Original documents with missing (NA) titles `r sum(is.na(df$author_name))` / `r nrow(df)` documents (`r round(100*mean(is.na(df$author_name)), 1)`%)


### Author's name (100a) - Top 20


```{r}
#| label = "summarytitle-author-name",
#| echo = FALSE,
#| message = FALSE,
#| warning = FALSE,
#| fig.width = 8,
#| fig.height = 6
library(ggplot2)
#library(comhis)

theme_set(theme_bw(base_size = 12))
p <- top_plot(df, "author_name", ntop = ntop, log10 = TRUE)+
  labs(y = "Entries (n)", title = "Top titles (Log values)")
print(p)
```



### Complete Dataset Overview for 700 field
There are 'r sum(is.na(df.orig[[field]]))' records with missing author name information in the original raw data.
-   [Unique accepted entries in original data](dataTable/data_table.html?path=https://a3s.fi/swift/v1/AUTH_3c0ccb602fa24298a6fe3ae224ca022f/fennica-container/output.tables/author_name_accepted_700.csv): `r length(unique(df700_harm[[field]]))`
-   Original documents with non-NA titles `r sum(!is.na(df700_harm[[field]]))` / `r nrow(df700_harm)` (`r round(100*mean(!is.na(df700_harm[[field]])), 1)`%)
-   Original documents with missing (NA) titles `r sum(is.na(df700_harm[[field]]))` / `r nrow(df700_harm)` documents (`r round(100*mean(is.na(df700_harm[[field]])), 1)`%)


### Author's name (700a) - Top 20


```{r}
#| label = "summarytitle-author-name-700",
#| echo = FALSE,
#| message = FALSE,
#| warning = FALSE,
#| fig.width = 8,
#| fig.height = 6
library(ggplot2)
#library(comhis)

theme_set(theme_bw(base_size = 12))
p <- top_plot(df700_harm, "author_name_700", ntop = ntop, log10 = TRUE)+
  labs(y = "Entries (n)", title = "Top titles (Log values)")
print(p)
```



### Subset Analysis: 1809-1917 100,a 

-   [Unique accepted entries for for 1809-1917 years](dataTable/data_table.html?path=https://a3s.fi/swift/v1/AUTH_3c0ccb602fa24298a6fe3ae224ca022f/fennica-container/output.tables/author_name_accepted_19.csv): `r length(unique(df_19[[field]]))`


Top-`r ntop` titles and their title counts for period 1809-1917.

The accompanying plot visually underscores the prominence of these authors, emphasizing the metric of the number of unique titles published by each author.



```{r}
#| label = "summarytitle-author-name19",
#| echo = FALSE,
#| message = FALSE,
#| warning = FALSE,
#| fig.width = 8,
#| fig.height = 6
library(ggplot2)
#library(comhis)

theme_set(theme_bw(base_size = 12))
p <- top_plot(df_19, "author_name", ntop = ntop, log10 = TRUE)+
  labs(y = "Entries (n)", title = "Top titles (Log values)")
print(p)
```



### Subset Analysis: 1809-1917 700,a 

-   [Unique accepted entries for for 1809-1917 years](dataTable/data_table.html?path=https://a3s.fi/swift/v1/AUTH_3c0ccb602fa24298a6fe3ae224ca022f/fennica-container/output.tables/author_name_accepted_700_19.csv): `r length(unique(df700_19[[field]]))`


Top-`r ntop` titles and their title counts for period 1809-1917.

The accompanying plot visually underscores the prominence of these authors, emphasizing the metric of the number of unique titles published by each author.



```{r}
#| label = "summarytitle-author-name-700-19",
#| echo = FALSE,
#| message = FALSE,
#| warning = FALSE,
#| fig.width = 8,
#| fig.height = 6
library(ggplot2)


theme_set(theme_bw(base_size = 12))
p <- top_plot(df700_19, "author_name_700", ntop = ntop, log10 = TRUE)+
  labs(y = "Entries (n)", title = "Top titles (Log values)")
print(p)
```




# Author Database

## Description



```{r}
# # Download the csv file
url <- "https://a3s.fi/swift/v1/AUTH_3c0ccb602fa24298a6fe3ae224ca022f/fennica-container/output.tables/all_author_info.csv"

author_database <- read.delim(
  url,
  quote = "",
  fill = TRUE,
  stringsAsFactors = FALSE,
  check.names = FALSE
)

author_database <- author_database %>%
  mutate(
    across(
      where(is.character),
      ~ {
        x <- trimws(.x)
        x[x %in% c("", "NA", "N/A", "NULL", "-")] <- NA_character_
        x
      }
    )
  )

author_database <- author_database %>%
  mutate(
    gender = trimws(gender),
    gender = na_if(gender, ""),
    gender = na_if(gender, "Unknown"),

    # Take the first value from semicolon-separated gender values
    gender_primary = trimws(sub("[|/].*$", "", gender)),
    gender_primary = na_if(gender_primary, ""),
    gender_primary = na_if(gender_primary, "Unknown")
  )
```




The author database was created by combining author information from MARC 100 (main authors) and MARC 700 (added authors) fields. Authors were identified primarily through Asteri authority identifiers when available. Records lacking authority identifiers were retained using harmonized author names.

The resulting database represents unique authors appearing in the Fennica dataset and serves as a foundation for author-level analyses and metadata enrichment. Author records were enriched using information obtained from Kanto authority data, including birth and death years, profession, language, country, field of activity, place associations, and related persons. Additional variables include inferred gender, publication activity measures, and the number of Fennica records associated with each author.

Publication activity was calculated from publication years recorded in Fennica and includes the first observed publication year, last observed publication year, mean publication year, and activity span.

## Complete Dataset Overview

Unique authors: `r unique(author_database$all_names)` (`r round(mean(!is.na(author_database$all_names)) * 100, 1)`%)

Authors with Asteri identifier: `r sum(!is.na(author_database$all_asteri))` (`r round(mean(!is.na(author_database$all_asteri)) * 100, 1)`%).

Authors with birth year information: `r sum(!is.na(author_database$all_dates))` (`r round(mean(!is.na(author_database$all_dates)) * 100, 1)`%).

Authors with death year information: `r sum(!is.na(author_database$till))` (`r round(mean(!is.na(author_database$till)) * 100, 1)`%).

Authors with calculated age: `r sum(!is.na(author_database$author_age))` (`r round(mean(!is.na(author_database$author_age)) * 100, 1)`%).

Authors with gender information: `r sum(!is.na(author_database$gender_primary))` (`r round(mean(!is.na(author_database$gender_primary)) * 100, 1)`%).

Authors with profession information: `r sum(!is.na(author_database$profession))` (`r round(mean(!is.na(author_database$profession)) * 100, 1)`%).

Authors with field of activity information: `r sum(!is.na(author_database$fieldOfActivityOfPerson))` (`r round(mean(!is.na(author_database$fieldOfActivityOfPerson)) * 100, 1)`%).

Authors with language information: `r sum(!is.na(author_database$language))` (`r round(mean(!is.na(author_database$language)) * 100, 1)`%).

Authors with country information: `r sum(!is.na(author_database$country))` (`r round(mean(!is.na(author_database$country)) * 100, 1)`%).

Authors with place information: `r sum(!is.na(author_database$place))` (`r round(mean(!is.na(author_database$place)) * 100, 1)`%).




# Top Authors' names



```{r}
#| label = "top_author_name",
#| echo = FALSE,
#| message = FALSE,
#| warning = FALSE,
#| fig.width = 8,
#| fig.height = 6
library(ggplot2)
#library(comhis)

theme_set(theme_bw(base_size = 12))
p <- top_plot(author_database, "all_names", ntop = ntop, log10 = TRUE)+
  labs(y = "Entries (n)", title = "Top author names (Log values)")
print(p)
```

